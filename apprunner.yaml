# apprunner.yaml
version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - npm install
    build:
      - npm run build # If you have a build step
run:
  command: npm start
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: "production"