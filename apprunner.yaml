version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - n=$(which node)
      - echo "Node path: $n"
      - node --version
      - npm --version
      - npm ci
    build:
      - echo "Build completed"
run:
  command: npm start
  network:
    port: 8080