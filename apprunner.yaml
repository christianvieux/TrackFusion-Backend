# apprunner.yaml
version: 1.0
runtime: nodejs18
build:
  commands:
    pre-build:
      - pwd
      - ls -la
      - cd Backend
      - pwd
      - ls -la
      - npm install
run:
  command: npm start
  network:
    port: 8080
  env:
    - name: NODE_ENV
      value: "production"